{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // SQLite – file is created next to the running binary.
    // For SQL Server replace with: "Server=.;Database=CleanArch26;Trusted_Connection=True;"
    "Default": "Data Source=cleanarch26.db"
  }
}
